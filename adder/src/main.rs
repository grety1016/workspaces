//! MyCrate
//! 
//! my_crate is main
//! 


#![allow(unused)]
#![allow(dead_code)]
use add_one;
use evaluate_max_num;
use struct_t;
use lifecycle;
use closure;
use smart_point;



fn main() {
   //////////////////////////add_one code
   // add_one::push_one()
   ////////////////////////////////////// 
   //////////////////////evaluate_max_num
   //evaluate_max_num::do_thing(); 
   //////////////////////////////////////
   /////////////////////////////struct_t
   //struct_t::struct_t();
   ////////////////////////////lifecycle
   //lifecycle::lifecycle()
   ///////////////////////////////////// 
   ////////////////////////////closure
   //closure::closure();
   /////////////////////////////////////  
   ///////////////////////////////smart_point
   smart_point::smart_point();
   ///////////////////////////////////// 
}
