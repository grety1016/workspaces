#![allow(unused)]
use add_one;
use evaluate_max_num;
use struct_t;



fn main() {
   //////////////////////////add_one code
   // add_one::push_one()
   ////////////////////////////////////// 
   //////////////////////evaluate_max_num
   //evaluate_max_num::do_thing(); 
   //////////////////////////////////////
   /////////////////////////////struct_t
   struct_t::struct_t();
   //////////////////////////////////////
}
